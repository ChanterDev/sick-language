namespace SickLanguage.Core {
    public enum Operator {
        PLUS,
        MINUS,
        MULTIPLY,
        DIVIDE,

        EQUALS,
        GREATER,
        GREATER_EQUALS,
        LESS,
        LESS_EQUALS,
        REJECT,
        REJECT_EQUALS,
        AND,
        OR,
    }
}